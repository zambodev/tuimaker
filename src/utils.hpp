#ifndef UTILS_HPP
#define UTILS_HPP

#include <tuple>
#include <vector>
#include "window.hpp"


namespace UTILS
{
    int getTerminalWidth(void);
    int getTerminalHeight(void);
    int getProgressiveId(void);
}

#endif
